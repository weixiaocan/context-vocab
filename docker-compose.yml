services:
  vocab-server:
    build: ./server
    ports:
      - "127.0.0.1:8001:8000"
    env_file:
      - ./server/.env
    volumes:
      - ./server/data:/app/data
      - ./server/config.yaml:/app/config.yaml:ro
    restart: unless-stopped
