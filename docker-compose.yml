services:
  vocab-server:
    build: ./server
    ports:
      - "8001:8000"
    env_file:
      - ./server/.env
    volumes:
      - ./server/data:/app/data
      - ./server/config.yaml:/app/config.yaml:ro
    restart: unless-stopped
